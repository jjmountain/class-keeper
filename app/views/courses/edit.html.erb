<main>
  <%= render partial: 'shared/page_header', locals: { title: "#{@course.name}", subtitle: 'Edit course', feather_icon: 'edit' } %>
  <div class="container-fluid mt-n10">
    <div class="card">
      <div class="card-body">
        <%= render partial: 'course_form', locals: { course: @course } %>
      </div>
    </div>
  </div>
</main>