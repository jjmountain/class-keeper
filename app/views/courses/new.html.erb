<%# <div class="container">
 <div class="row my-4">
    <div class="col-md-6 offset-md-3"> %>

<main>
  <%= render partial: 'shared/page_header', locals: { title: 'New Course', feather_icon: 'plus' } %>

  <div class="container-fluid mt-n10">

    <div class="card">
      <div class="card-header">Set up your new course</div>
      <div class="card-body">
        <%= render partial: 'course_form', locals: { course: @course } %>
      </div>
    </div>
  </div>
</main>
    <%# </div>
  </div>
</div> %>
